{
  "buildCommand": "cd club-events-portal && npm install && npm run build",
  "outputDirectory": "club-events-portal/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
